/// <reference types="vite/client" />

/**
 * Injected by Vite's `define` from package.json.version.
 * Used in the footer to keep the displayed version in sync with the npm package.
 */
declare const __APP_VERSION__: string;
